/// <reference types="@react-three/fiber" />

// Ensures React Three Fiber's JSX intrinsic elements (mesh, group,
// planeGeometry, meshStandardMaterial, etc.) are globally available to
// TypeScript across all studio components.
import '@react-three/fiber';
